<% content_for(:container_class) { "narrow" } %>

<h1>Edit Profile</h1>

<div class="card">
  <%= render "my/profiles/form", profile: @profile %>
</div>

<p><%= link_to "← Back to profile", my_profile_path(@profile) %></p>
